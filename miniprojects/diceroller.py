# dice roller which rolls a dice and returns the result and counts the number of times each number is rolled
import random as ran
def roll_dice():
    results = []
    number_rolled = ran.randint(1, 6)
    
    
    
    